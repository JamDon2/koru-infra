apiVersion: apps/v1
kind: Deployment
metadata:
  name: koru-web
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: koru-web
  template:
    metadata:
      labels:
        app: koru-web
    spec:
      imagePullSecrets:
        - name: ghcr-creds
      containers:
        - name: koru-web
          image: "ghcr.io/jamdon2/koru-web:sha-d690d86"
          ports:
            - containerPort: 8000
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
